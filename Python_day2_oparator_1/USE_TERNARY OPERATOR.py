# Declare two variables
a = 10
b = 20

# Use a ternary operator to determine which variable is larger
largest = a if a > b else b

# Print the result
print(f"The largest number is: {largest}")
